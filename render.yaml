services:
  - type: web
    name: pcb-temperature-app
    runtime: python312
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /pcb_temperature_app.html
